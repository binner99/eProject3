@model IEnumerable<Data.Address>

@{
    ViewBag.Title = " Address";
}

<p>
    @Html.ActionLink(" Add New", "Create", null, new { @class = "btn btn-link fa fa-plus" })
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.compName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.compAdress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.image)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.compName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.compAdress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.image)
        </td>
        <td>
            @Html.ActionLink(" Edit", "Edit", new { id = item.addressID }, new { @class = "btn btn-sm btn-primary fa fa-edit" })
            @Html.ActionLink(" Details", "Details", new { id = item.addressID }, new { @class = "btn btn-sm btn-info fa fa-info" })
            @Html.ActionLink(" Delete", "Delete", new { id = item.addressID }, new { @class = "btn btn-sm btn-danger fa fa-remove" })
        </td>
    </tr>
}

</table>
